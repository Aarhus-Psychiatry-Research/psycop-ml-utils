"""pytest CI fails if there are no tests.

This is a dummy test to make sure pytest passes and so we can keep it
for new functions that do require a test
"""


def test_nothing():  # pylint: disable=missing-function-docstring
    assert True
