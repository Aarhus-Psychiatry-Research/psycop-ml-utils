on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - name: Release please
      - uses: google-github-actions/release-please-action@v3
        with:
          release-type: python
          package-name: psycopmlutils